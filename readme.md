
---

## **Setup**

1. Clone the repository:
```bash
git clone <repo_url>
cd rag_chatbot
Install dependencies:

bash
Copy code
pip install -r requirements.txt
Create a .env file in the root directory:

dotenv
Copy code
OPENAI_API_KEY=your_openai_api_key_here
Usage
1️⃣ CLI Chatbot
bash
Copy code
python main.py
2️⃣ Web Chatbot (Streamlit)
bash
Copy code
streamlit run app.py
